<Application x:Class="MyWpf.App"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:local="clr-namespace:MyWpf"
             StartupUri="MainWindow.xaml">
             <!-- DispatcherUnhandledException='App_DispatcherUnhandledException'> -->
             <!-- clr-namespace 在程序集中声明的 CLR 命名空间，此程序集包含要作为元素公开的公共类型。
             assembly= 包含部分或全部引用 CLR 命名空间的程序集。
              <party:Balloon />。该前缀表明该元素存在于与默认命名空间不同的 XAML 命名空间中，你必须将一个 XAML 命名空间映射到前缀 party，然后才能使用此元素。
              d: (http://schemas.microsoft.com/expression/blend/2008)"d:" XAML 命名空间旨在提供设计器支持 -->
    <Application.Resources>
         
    </Application.Resources>
</Application>
